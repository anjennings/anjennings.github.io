import { RepoDirectiveDirective } from './repo-directive.directive';

describe('RepoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
